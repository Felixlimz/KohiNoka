<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.picodiploma.kohinoka">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KohiNoka">
        <activity android:name=".NokaPedia.DetailDuaPediaActivity"></activity>
        <activity android:name=".NokaPedia.HomePediaActivity" />
        <activity android:name=".video.VideoListActivity" />
        <activity android:name=".scan.ScanActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".onboarding.ThreeActivity" />
        <activity android:name=".onboarding.TwoActivity" />
        <activity android:name=".onboarding.OneActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".onboarding.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NokaPedia.DetailPediaActivity"
            android:label="@string/title_activity_detail" />
    </application>

</manifest>